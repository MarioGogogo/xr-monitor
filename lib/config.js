/**
 * 阿里云日志上报系统这里不做处理
 * @type {string}
 */
let host = 'cn-beijing.log.aliyuncs.com';
let project = 'zhufengmonitor';
let logStore = 'zhufengmonitor-store';

//TODO:解决配置url参数
export const BASE_URL = 'https://egg-v1.fml.ink/logs';
// this.url = `http://${project}.${host}/logstores/${logStore}/track`; //阿里云上报的路径
